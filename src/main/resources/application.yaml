spring:
  application:
    name: ReactiveEcomExample
  r2dbc:
    url: r2dbc:mysql://localhost:3306/springReactiveDbDemo
    username: ims
    password: ims123

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/myRealm

server:
  port: 7001

